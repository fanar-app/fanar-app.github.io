---
const rows = [
  { without: 'console.log everywhere',      with: 'Structured, typed payloads' },
  { without: 'Remove before committing',    with: 'Always-on, stays in dev only' },
  { without: 'No SQL visibility',           with: 'SQL with syntax highlight + timing' },
  { without: 'No timing data',              with: 'Named timers built-in' },
  { without: 'Stack traces buried in logs', with: 'Clickable frames — opens VS Code' },
];
---

<section class="border-b border-[var(--border)]">
  <div class="border-b border-[var(--border)] px-10 lg:px-16 py-10">
    <div class="text-[var(--accent-text)] text-xs font-mono mb-4">&lt; The problem &gt;</div>
    <h2 class="text-4xl lg:text-5xl font-bold text-[var(--text)] leading-tight">
      The Old Way vs.<br />The Fanar Way.
    </h2>
  </div>

  <div class="grid grid-cols-2 border-b border-[var(--border)]">
    <div class="border-r border-[var(--border)] px-10 lg:px-12 py-4">
      <span class="text-xs font-mono text-red-400 tracking-widest">WITHOUT FANAR</span>
    </div>
    <div class="px-10 lg:px-12 py-4">
      <span class="text-xs font-mono text-[var(--accent-text)] tracking-widest">WITH FANAR</span>
    </div>
  </div>

  {rows.map(row => (
    <div class="grid grid-cols-2 border-b border-[var(--border)] last:border-b-0">
      <div class="border-r border-[var(--border)] px-10 lg:px-12 py-5 flex items-center gap-4">
        <span class="text-red-400 shrink-0 font-mono font-bold">✕</span>
        <span class="font-mono text-sm text-[var(--text-dim)]">{row.without}</span>
      </div>
      <div class="px-10 lg:px-12 py-5 flex items-center gap-4">
        <span class="text-[var(--green)] shrink-0 font-mono font-bold">✓</span>
        <span class="font-mono text-sm text-[var(--text)]">{row.with}</span>
      </div>
    </div>
  ))}
</section>
